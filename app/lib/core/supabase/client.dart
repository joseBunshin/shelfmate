// Supabase client surface. U1.3 wires Supabase.initialize() in main()
// against Env values; this file holds the Riverpod provider that exposes
// the SupabaseClient to the rest of the app.

// TODO(U1.3): Initialize Supabase.initialize() in main() before runApp().
// TODO(U1.3): Expose `supabaseClientProvider` here.
