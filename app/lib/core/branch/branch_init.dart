// Branch SDK init for deferred deep links (R3, R13, R14).
//
// Three link types per Phase 4: rec links, profile links, friend invite links.
// Match-rate falsification trigger flagged in plan Open Questions —
// instrument matched/unmatched ratio in U3 implementation.

// TODO(U1.3): FlutterBranchSdk.init(...) before runApp().
// TODO(U3): getInitialReferringParams() handler + Branch.subscribe() listener.
